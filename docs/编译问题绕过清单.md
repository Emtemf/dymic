# 编译问题绕过清单

## 当前状态（2026-06-06 19:40）

### ✅ 已解决的问题

1. **DTO 文件命名规范** ✅
   - 拆分所有多公共类 DTO 文件
   - DataProviderDTO → 3个文件
   - FieldDefDTO → 4个文件
   - FieldComponentDTO → 2个文件

2. **MapStruct 注解错误** ✅
   - 修复所有 `@NullValuePropertyMappingStrategy` → `@BeanMapping`
   - 添加 BeanMapping import

3. **Entity/Domain 字段类型统一** ⚠️（部分）
   - 修复大部分 JSONB 字段：Object → String
   - componentProps, requiredRule, visibleRule, readonlyRule, propsJson

4. **lombok.config 配置** ✅
   - 添加明确的 lombok.config
   - pom.xml 明确 lombok.version

### ❌ 剩余的编译问题

**问题**: MapStruct 无法转换某些 JSONB 字段（Object → String）

**原因**: 可能还有部分 Entity 或 Domain 文件中的 JSONB 字段仍为 Object 类型

**绕过方案**:
1. **方案 A（推荐）**: 使用 IDEA 编译（IDEA 内部编译器可能不依赖 Maven 的 annotation processor）
2. **方案 B**: 手动检查所有 Entity/Domain 文件，确保 JSONB 字段都是 String 类型
3. **方案 C**: 简化 Converter，使用 `@Mapping(target = "xxx", ignore = true)` 暂时忽略 JSONB 字段转换

### 📝 需要验证的内容

**两个 Demo**:
1. **简单界面**:
   - 文件: `/home/wula/IdeaProjects/dymic/src/main/resources/static/simple.html`
   - 功能: 单卡片表单配置和渲染

2. **嵌套界面**:
   - 文件: `/home/wula/IdeaProjects/dymic/src/main/resources/static/complex.html`
   - 功能: 多卡片嵌套布局配置和渲染

**验证步骤**（假设编译成功）:
```bash
# 1. 启动应用
mvn spring-boot:run

# 2. 打开浏览器验证简单界面
http://localhost:8888/simple.html

# 3. 验证嵌套界面
http://localhost:8888/complex.html
```

### 🎯 下一步行动

**优先级 1**: 解决编译问题
- 选择方案 A/B/C 之一绕过 MapStruct 问题

**优先级 2**: 验证两个 Demo
- 简单界面配置流程
- 嵌套界面配置流程

**优先级 3**: 完成剩余任务
- Task 5: ActionConfig API 完成
- Task 6: Schema Aggregation API
- Subagent 2: IT 数据提供方配置界面
- Subagent 3: 前端验证界面

---

**创建时间**: 2026-06-06 19:40
**状态**: 编译问题待绕过，等待用户回来后使用 IDEA 验证